# Local intake log, written at runtime. Personal data, not source.
water_tracker_state.json
water_tracker_state.json.tmp
water_tracker_state.json.corrupt

__pycache__/
*.pyc

# Tool caches. Both write their own .gitignore inside themselves, so the
# contents were already ignored; these entries cover the directories too.
.mypy_cache/
.pytest_cache/

.DS_Store

# Checker caches and the venv from requirements-dev.txt
.mypy_cache/
.pylint.d/
.venv/
